version: '3.8'

services:
  yt-dlp_ui:
    build: .
    image: yt-dlp_ui
    container_name: yt-dlp_ui
    ports:
      - "8000:8000"
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
    environment:
      - DOWNLOADS_DIR=/app/downloads
